/**
 * User configuration for the finance splitter application
 * This file centralizes all user-related configurations to make the app more maintainable
 */

export const USER_CONFIG = {
    // Primary users - can be changed based on actual users
    PRIMARY_USER_1: 'Ruby', 
    PRIMARY_USER_2: 'Jack',
    
    // Label for when expenses are split between both users
    BOTH_LABEL: 'Both',
    
    // Default labels array used throughout the application
    DEFAULT_LABELS: ['Ruby', 'Jack', 'Both'],
  };